import type { Metadata } from 'next';
import { Geist, Geist_Mono } from 'next/font/google';
import './globals.css';

const geistSans = Geist({
  variable: '--font-geist-sans',
  subsets: ['latin'],
});

const geistMono = Geist_Mono({
  variable: '--font-geist-mono',
  subsets: ['latin'],
});

export const metadata: Metadata = {
  title: 'KASTORAS | AI-powered Construction Intelligence Platform',
  description:
    'Estimate construction costs, generate quantities and BOQs, and understand embodied carbon from one intelligent construction platform. From Drawings to Decisions.',
};

export default function RootLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return (
    <html
      lang="en"
      className={`${geistSans.variable} ${geistMono.variable} h-full antialiased`}
    >
      <body className="h-full bg-[#F8FAFC] text-slate-900 font-sans">{children}</body>
    </html>
  );
}
